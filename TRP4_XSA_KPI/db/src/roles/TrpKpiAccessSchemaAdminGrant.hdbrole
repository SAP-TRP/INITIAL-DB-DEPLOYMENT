{
    "role": {
        "name": "sap.tm.trp.roles::TrpKpiAccessSchemaAdmin#",
        "schema_privileges": [{
		   "privileges_with_grant_option": ["SELECT METADATA", "CREATE ANY", "SELECT CDS METADATA", "SELECT", "INSERT", "UPDATE", "EXECUTE", "DELETE", "ALTER", "CREATE TEMPORARY TABLE" ]
		}]
    }
}
